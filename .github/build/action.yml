name: "Build Book"
description: "Build Book using Enki"
author: "staxly@openstax.com"

inputs:
  REPO_NAME:
    description: "Name of the repository to build"
    required: true
  REPO_SHORT_NAME:
    description: "Short Name of the repository to build"
    required: true
  REPO_PATH:
    description: "Path of the cloned repository"
    required: true

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "book"
  color: "purple"
